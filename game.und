introduction (
    page (
        image ()
        description ()
    )
)
first location (test1)
locations (
    test1 (
        background ()
        desciption ()
        render (
            if (equal (get (varname)) (get (varname))) (
                overlay (
                    image ()
                    description ()
                )
            )
        )
        spin (
            play sound (spin)
        )
    )
)
