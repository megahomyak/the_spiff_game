engine._render world {
let regions = []
ctx.curloc().render(draw=(region) => {
regions.push(region)
})
output.draw(ctx.curloc().bg, regions)
}

engine.handle interaction {
ctx.curloc()[args.interaction](play sound=(sound) => {

}, switch location=(new location name) => {

})
}
