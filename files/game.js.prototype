Sound {
    play() - plays the sound
}

Output {
    screen {
        (void) draw(list of images with coordinates) - redraws the scene with the specified images
    }
    audio {
        (Sound) preload(sound name) - preloads a sound with the specified name
    }
}

Engine {
    -> (Output) output

}
