sound() {
    play() - plays the sound
}

image() {
    unit_interval top()
    unit_interval bottom()
    unit_interval left()
    unit_interval right()
    string 
}

engine_context() {
    screen() {
        draw(list<image>) - redraws the scene with the specified images
    }
    audio() {
        (sound) preload(sound name) - preloads a sound with the specified name
    }
}

engine(engine context) {

}
